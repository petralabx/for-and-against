# Pricing history

Dated snapshots written by the price-watch GitHub Action (one file per run, `YYYY-MM-DD.json`). Append-only — this record powers `docs/pricing-report.md` and change alerts. Until the Action is live, manual snapshots may be dropped here using the same naming.
