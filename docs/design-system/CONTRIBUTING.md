# Contributing — For & Against

1. Wrap new routes in `.brand-for-and-against`.
2. Use --fa- tokens only — no raw hex in TSX/CSS.
3. Run `python3 scripts/check-brand-repo-structure.py` before opening a PR.
